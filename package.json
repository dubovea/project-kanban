{
  "name": "project-kanban",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c cyan,green \"npm run dev -w @project-kanban/api\" \"npm run dev -w @project-kanban/web\"",
    "build": "npm run build -w @project-kanban/shared && npm run build -w @project-kanban/api && npm run build -w @project-kanban/web",
    "typecheck": "npm run typecheck -w @project-kanban/shared && npm run typecheck -w @project-kanban/api && npm run typecheck -w @project-kanban/web"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "typescript": "^6.0.3"
  }
}
